import {all, fork} from 'redux-saga/effects';
import {
  handleLogin,
  handleSignup,
  handleForgetPassword,
  handleLogout,
  handleRecoverPassword,
  handleSignupCheck,
} from './AuthenticationSaga';
import {
  handleChangePassword,
  handleSaveAdress,
  handleSaveCredit, 
  handleSaveProfile,
  handleUploadPicture,
  handleGetAddress,
  handleGetCredit,
  handleGetProfile,
  handleEditAddress,
  handleDeleteAddress,
  handleEditCredit,
  handleDeleteCredit,
  handleGetBooking,
  handleGetOrders,
  handleGetVouchers,
  handleGetRatings,
  handleEditRatings,
  handleDeleteRating,
  handleGetFavs,
  handleAddFav,
  handleRemoveFav,
  handleAddRatings
} from './ProfileSaga';
import {
 handleRestaurantSignup, 
 handleAddPayment, 
 handleGetCuisine, 
 handleGetAllPackages, 
 handleGetRestaurantInfo,
 handleGetRestaurants, 
 handleGetMenu,
 handleGetPublishKey,
 handlePlaceOrder,
 handleGetClientKey,
 handleSearch,
handleGetOpening,
handleBookTable,
handleGetVoucher,
handleVoucherEdit,
handleMerhantRating
} from './RestaurantSaga';
export default function* RootSaga() {
  yield all([
    fork(handleLogin),
    fork(handleSignup),
    fork(handleSignupCheck),
    fork(handleLogout),
    fork(handleForgetPassword),
    fork(handleRecoverPassword),
    fork(handleChangePassword),
    fork(handleSaveAdress),
    fork(handleSaveCredit),
    fork(handleSaveProfile),
    fork(handleUploadPicture),
    fork(handleRestaurantSignup),
    fork(handleAddPayment),
    fork(handleGetAddress),
    fork(handleGetCredit),
    fork(handleGetProfile),
    fork(handleGetCuisine),
    fork(handleEditAddress),
    fork(handleDeleteAddress),
    fork(handleEditCredit),
    fork(handleDeleteCredit),
    fork(handleUploadPicture),
    fork(handleGetAllPackages),
    fork(handleGetRestaurantInfo),
    fork(handleGetRestaurants),
    fork(handleGetMenu),
    fork(handleGetPublishKey),
    fork(handleGetClientKey),
    fork(handlePlaceOrder),
    fork(handleSearch),
    fork(handleGetOpening),
    fork(handleBookTable),
    fork(handleGetBooking),
    fork(handleGetOrders),
    fork(handleGetVouchers),
    fork(handleGetRatings),
    fork(handleEditRatings),
    fork(handleDeleteRating),
    fork(handleGetVoucher),
    fork(handleVoucherEdit),
    fork(handleGetFavs),
    fork(handleAddFav),
    fork(handleRemoveFav),
    fork(handleAddRatings),
    fork(handleMerhantRating),
  ]);
};
